CREATE TABLE Vendas (
    CupomID INT,
    ProdutoID INT,
    Quantidade INT,
    Valor DECIMAL(10, 2),
    Desconto DECIMAL(10, 2),
    Custo DECIMAL(10, 2),
    ValorLiquido DECIMAL(10, 2)
);
